Turn the timeline into the sole replacement summary. The timeline uses the history line format described below; `your_*` records are mine.

Include what I wrote, created, changed, and achieved with tools. Include user requests, environment events, and tool results that changed understanding. Skip routine calls, acknowledgements, and blow-by-blow detail.

Use terse, one-line bullets. First person. Chronological. Preserve useful names, thread IDs, dates, paths, URLs, and environment names inline. Content beats metadata.

Merge relevant facts from any earlier `summary` record. Cover both my own records and the outside world. State file changes as path + change and tool outcomes as tool + result.

Only summarize. Never answer, react to, or continue the timeline.
