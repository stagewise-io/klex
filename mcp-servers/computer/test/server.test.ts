import { describe, it, expect } from "vitest";

describe("package config", () => {
  it("has a name", () => {
    // Placeholder test — real tests will hit the MCP server endpoints
    expect(true).toBe(true);
  });
});
